# Руководство по интеграции и запуску "The Last of Guss"

> Полное пошаговое руководство по настройке, интеграции и запуску игрового проекта

## Содержание

1. [Требования к системе](#требования-к-системе)
2. [Архитектура проекта](#архитектура-проекта)
3. [Установка зависимостей](#установка-зависимостей)
4. [Настройка базы данных PostgreSQL](#настройка-базы-данных-postgresql)
5. [Конфигурация переменных окружения](#конфигурация-переменных-окружения)
6. [Миграции базы данных](#миграции-базы-данных)
7. [Запуск приложения](#запуск-приложения)
8. [Проверка интеграции](#проверка-интеграции)
9. [Решение типичных проблем](#решение-типичных-проблем)

---

## Требования к системе

### Обязательное ПО

- **Node.js**: версия 18.x или выше
- **npm**: версия 9.x или выше (устанавливается с Node.js)
- **PostgreSQL**: версия 14.x или выше
- **Git**: для клонирования репозитория

### Проверка версий

```bash
node --version   # должно быть >= v18.0.0
npm --version    # должно быть >= 9.0.0
psql --version   # должно быть >= 14.x
```

---

## Архитектура проекта

### Структура директорий

```
goose/
├── backend/              # NestJS backend сервер
│   ├── src/
│   │   ├── auth/        # Модуль аутентификации
│   │   ├── rounds/      # Модуль управления раундами
│   │   ├── taps/        # Модуль обработки тапов
│   │   ├── users/       # Модуль пользователей
│   │   ├── prisma/      # Prisma service
│   │   ├── common/      # Общие утилиты и декораторы
│   │   └── main.ts      # Точка входа
│   ├── prisma/
│   │   └── schema.prisma # Схема БД
│   ├── .env             # Переменные окружения (создается вами)
│   └── package.json
│
├── frontend/            # React + Vite фронтенд
│   ├── src/
│   │   ├── components/  # React компоненты
│   │   ├── context/     # React Context (Auth)
│   │   ├── pages/       # Страницы приложения
│   │   ├── services/    # API сервисы
│   │   ├── types/       # TypeScript типы
│   │   └── utils/       # Утилиты
│   ├── .env             # Переменные окружения (создается вами)
│   └── package.json
│
├── docs/                # Документация
└── README.md            # Корневой readme
```

### Стек технологий

**Backend:**
- NestJS 10.3 (Node.js framework)
- Prisma 5.8 (ORM)
- PostgreSQL (база данных)
- JWT (аутентификация)
- bcrypt (хеширование паролей)

**Frontend:**
- React 18.3
- TypeScript 5.6
- Vite 5.4 (сборщик)
- Axios (HTTP клиент)
- Tailwind CSS (стилизация)
- Radix UI (UI компоненты)
- React Router (маршрутизация)

---

## Установка зависимостей

### 1. Backend зависимости

```bash
cd /Users/sunshad0w/Work/tests/goose/backend
npm install
```

**Что устанавливается:**
- NestJS core и платформа Express
- Prisma Client и CLI
- JWT и Passport для аутентификации
- bcrypt для хеширования паролей
- class-validator и class-transformer для валидации

### 2. Frontend зависимости

```bash
cd /Users/sunshad0w/Work/tests/goose/frontend
npm install
```

**Что устанавливается:**
- React и React DOM
- Vite для сборки и dev-сервера
- Axios для API запросов
- Tailwind CSS и PostCSS
- Radix UI компоненты
- React Router для навигации

---

## Настройка базы данных PostgreSQL

### Вариант 1: Установка PostgreSQL локально (macOS)

#### Через Homebrew:

```bash
# Установка PostgreSQL
brew install postgresql@14

# Запуск службы PostgreSQL
brew services start postgresql@14

# Проверка статуса
brew services list | grep postgresql
```

#### Создание базы данных:

```bash
# Войти в PostgreSQL CLI
psql postgres

# В psql CLI выполнить:
CREATE DATABASE goose_game;
CREATE USER goose_user WITH PASSWORD 'goose_password_2024';
GRANT ALL PRIVILEGES ON DATABASE goose_game TO goose_user;

# Для PostgreSQL 15+ также нужно:
\c goose_game
GRANT ALL ON SCHEMA public TO goose_user;

# Выход из psql
\q
```

### Вариант 2: PostgreSQL через Docker

```bash
# Запустить PostgreSQL в контейнере
docker run --name goose-postgres \
  -e POSTGRES_DB=goose_game \
  -e POSTGRES_USER=goose_user \
  -e POSTGRES_PASSWORD=goose_password_2024 \
  -p 5432:5432 \
  -d postgres:14

# Проверить, что контейнер запущен
docker ps | grep goose-postgres
```

### Проверка подключения:

```bash
# Подключиться к БД для проверки
psql -h localhost -U goose_user -d goose_game

# Если подключение успешно, вы увидите:
# goose_game=#
```

---

## Конфигурация переменных окружения

### Backend .env файл

Создайте файл `/Users/sunshad0w/Work/tests/goose/backend/.env`:

```env
# Database Configuration
DATABASE_URL="postgresql://goose_user:goose_password_2024@localhost:5432/goose_game"

# JWT Configuration
JWT_SECRET="your-super-secret-jwt-key-change-in-production-12345678"
JWT_EXPIRATION="24h"

# Server Configuration
PORT=3000
NODE_ENV="development"

# CORS Configuration
FRONTEND_URL="http://localhost:5173"

# Rate Limiting
RATE_LIMIT_TAPS_PER_SECOND=10
RATE_LIMIT_GLOBAL_PER_MINUTE=100

# Round Configuration
ROUND_DURATION=60
COOLDOWN_DURATION=30
```

**Важные параметры:**

- `DATABASE_URL` - строка подключения к PostgreSQL
  - Формат: `postgresql://USER:PASSWORD@HOST:PORT/DATABASE`
  - Убедитесь, что данные совпадают с созданной БД

- `JWT_SECRET` - секретный ключ для подписи JWT токенов
  - **ВАЖНО**: Используйте сложный уникальный ключ в production!
  - Минимум 32 символа

- `FRONTEND_URL` - URL фронтенда для настройки CORS
  - В dev режиме: `http://localhost:5173`
  - В production: ваш production URL

- `ROUND_DURATION` - длительность игрового раунда в секундах (60 = 1 минута)
- `COOLDOWN_DURATION` - длительность перерыва между раундами в секундах

### Frontend .env файл

Создайте файл `/Users/sunshad0w/Work/tests/goose/frontend/.env`:

```env
# Backend API URL
VITE_API_URL=http://localhost:3000
```

**Важно:**
- Все переменные окружения Vite должны начинаться с `VITE_`
- URL должен указывать на backend сервер без завершающего слеша

---

## Миграции базы данных

После настройки PostgreSQL и создания `.env` файла, примените миграции Prisma:

### 1. Генерация Prisma Client

```bash
cd /Users/sunshad0w/Work/tests/goose/backend
npm run prisma:generate
```

**Что происходит:**
- Генерируется типизированный Prisma Client на основе schema.prisma
- Создаются TypeScript типы для работы с БД

### 2. Применение миграций

```bash
npm run prisma:migrate
```

**Что происходит:**
- Создаются таблицы в базе данных:
  - `users` - пользователи игры
  - `rounds` - игровые раунды
  - `round_stats` - статистика игроков в раундах
- Применяются индексы для оптимизации запросов
- Настраиваются связи между таблицами

**Ожидаемый вывод:**

```
✔ Generated Prisma Client
✔ The migration has been applied successfully
```

### 3. (Опционально) Prisma Studio

Для просмотра и редактирования данных через UI:

```bash
npm run prisma:studio
```

Откроется браузер с адресом `http://localhost:5555` - визуальный редактор БД.

---

## Запуск приложения

### Вариант 1: Ручной запуск

#### Запуск Backend

```bash
# В первом терминале
cd /Users/sunshad0w/Work/tests/goose/backend
npm run start:dev
```

**Ожидаемый вывод:**

```
[Nest] INFO [NestFactory] Starting Nest application...
[Nest] INFO [InstanceLoader] AppModule dependencies initialized
🚀 Сервер запущен на http://localhost:3000
📚 Документация API: см. docs/api.md
```

#### Запуск Frontend

```bash
# Во втором терминале
cd /Users/sunshad0w/Work/tests/goose/frontend
npm run dev
```

**Ожидаемый вывод:**

```
  VITE v5.4.9  ready in 523 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

### Вариант 2: Использование скриптов

В корне проекта есть удобные скрипты для запуска:

```bash
# Запустить backend
./start-backend.sh

# Запустить frontend (в новом терминале)
./start-frontend.sh
```

### Первоначальная настройка (один раз)

Для автоматической установки всех зависимостей и настройки проекта:

```bash
cd /Users/sunshad0w/Work/tests/goose
./setup-project.sh
```

Этот скрипт:
1. Проверит наличие Node.js и npm
2. Установит backend зависимости
3. Установит frontend зависимости
4. Проверит наличие .env файлов
5. Сгенерирует Prisma Client
6. Применит миграции БД

---

## Проверка интеграции

### 1. Проверка Backend

После запуска backend, проверьте доступность API:

```bash
# Проверка health endpoint
curl http://localhost:3000

# Ожидается ответ: {"message": "The Last of Guss API"}
```

### 2. Проверка Frontend

1. Откройте браузер: `http://localhost:5173`
2. Вы должны увидеть страницу логина
3. Попробуйте зарегистрироваться:
   - Username: `testuser`
   - Password: `password123`

### 3. Проверка подключения Frontend -> Backend

Откройте Dev Tools в браузере (F12):
- **Network tab**: после регистрации/логина должны быть запросы к `http://localhost:3000/auth/*`
- **Console**: не должно быть CORS ошибок

### 4. Проверка базы данных

```bash
# Подключиться к БД
psql -h localhost -U goose_user -d goose_game

# Проверить созданные таблицы
\dt

# Проверить созданных пользователей
SELECT id, username, role, created_at FROM users;

# Выход
\q
```

---

## Решение типичных проблем

### Проблема: CORS ошибки в браузере

**Симптомы:**
```
Access to XMLHttpRequest at 'http://localhost:3000/auth/login'
from origin 'http://localhost:5173' has been blocked by CORS policy
```

**Решение:**
1. Проверьте, что backend `.env` файл содержит:
   ```env
   FRONTEND_URL="http://localhost:5173"
   ```
2. Перезапустите backend сервер
3. Убедитесь, что в `src/main.ts` включен CORS

---

### Проблема: "Ошибка подключения к БД"

**Симптомы:**
```
Error: Can't reach database server at `localhost:5432`
```

**Решение:**

1. Проверьте, что PostgreSQL запущен:
   ```bash
   # Для Homebrew
   brew services list | grep postgresql

   # Для Docker
   docker ps | grep goose-postgres
   ```

2. Проверьте DATABASE_URL в `.env`:
   ```bash
   cd /Users/sunshad0w/Work/tests/goose/backend
   cat .env | grep DATABASE_URL
   ```

3. Попробуйте подключиться вручную:
   ```bash
   psql -h localhost -U goose_user -d goose_game
   ```

---

### Проблема: "Prisma Client не найден"

**Симптомы:**
```
Error: @prisma/client did not initialize yet
```

**Решение:**
```bash
cd /Users/sunshad0w/Work/tests/goose/backend
npm run prisma:generate
```

---

### Проблема: Порт 3000 или 5173 уже занят

**Симптомы:**
```
Error: listen EADDRINUSE: address already in use :::3000
```

**Решение:**

Найти и остановить процесс, занимающий порт:

```bash
# Для порта 3000 (backend)
lsof -ti:3000 | xargs kill -9

# Для порта 5173 (frontend)
lsof -ti:5173 | xargs kill -9
```

Или изменить порт в `.env`:
```env
PORT=3001  # вместо 3000
```

---

### Проблема: JWT токен не работает

**Симптомы:**
- Постоянный redirect на /login
- Ошибки 401 Unauthorized

**Решение:**

1. Очистите localStorage в браузере:
   ```javascript
   // В консоли браузера
   localStorage.clear()
   ```

2. Проверьте JWT_SECRET в backend `.env`
3. Зарегистрируйтесь заново

---

### Проблема: Frontend не может подключиться к Backend

**Симптомы:**
```
Network Error
ERR_CONNECTION_REFUSED
```

**Решение:**

1. Убедитесь, что backend запущен:
   ```bash
   curl http://localhost:3000
   ```

2. Проверьте VITE_API_URL в frontend `.env`:
   ```bash
   cd /Users/sunshad0w/Work/tests/goose/frontend
   cat .env
   ```

3. Должно быть: `VITE_API_URL=http://localhost:3000`

---

## Полезные команды

### Backend

```bash
# Development режим с hot-reload
npm run start:dev

# Production build
npm run build
npm run start:prod

# Запуск тестов
npm run test

# Форматирование кода
npm run format

# Линтинг
npm run lint

# Prisma Studio (GUI для БД)
npm run prisma:studio

# Создание новой миграции
npx prisma migrate dev --name migration_name
```

### Frontend

```bash
# Development сервер
npm run dev

# Production build
npm run build

# Preview production build
npm run preview

# Линтинг
npm run lint
```

### Database

```bash
# Подключение к БД
psql -h localhost -U goose_user -d goose_game

# Backup БД
pg_dump -h localhost -U goose_user goose_game > backup.sql

# Восстановление БД
psql -h localhost -U goose_user goose_game < backup.sql

# Пересоздание БД (ОСТОРОЖНО!)
dropdb -h localhost -U goose_user goose_game
createdb -h localhost -U goose_user goose_game
cd backend && npm run prisma:migrate
```

---

## Следующие шаги

После успешного запуска:

1. Создайте первого пользователя через UI
2. Войдите в систему
3. Если вы admin - создайте первый раунд
4. Протестируйте функционал тапов
5. Проверьте работу статистики

---

## Дополнительная документация

- **Backend API**: `/Users/sunshad0w/Work/tests/goose/backend/README.md`
- **Frontend**: `/Users/sunshad0w/Work/tests/goose/frontend/README.md`
- **Схема БД**: `/Users/sunshad0w/Work/tests/goose/backend/prisma/schema.prisma`

---

## Поддержка

При возникновении проблем:

1. Проверьте логи backend и frontend
2. Откройте Dev Tools в браузере (Network и Console)
3. Проверьте статус PostgreSQL
4. Убедитесь, что все переменные окружения настроены правильно

---

**Версия документа:** 1.0.0
**Дата обновления:** 2025-11-17
