# UI/UX Дизайн - The Last of Guss

> **Краткая сводка UI/UX дизайна. Полная документация доступна в prd.md**

**Версия:** 1.0
**Дата:** 2025-11-17

---

## 1. Технологический стек

### Frontend
- **Framework:** React + TypeScript (strict mode) + Vite
- **Routing:** React Router v6
- **UI библиотека:** shadcn/ui + Radix UI
- **Styling:** Tailwind CSS
- **Toast уведомления:** sonner (~5kb)
- **Состояние:** Context API
- **HTTP клиент:** Axios

### Установка UI компонентов
```bash
npx shadcn-ui@latest init
npx shadcn-ui@latest add button input card form toast dialog
npm install sonner
```

---

## 2. Design System

### 2.1 Цветовая палитра

```typescript
// tailwind.config.js - CSS Variables
colors: {
  primary: {
    DEFAULT: "#2196F3",      // Синий акцент (кнопки, ссылки)
    foreground: "#FFFFFF",   // Текст на primary фоне
  },
  success: "#4CAF50",        // Зеленый (Active раунд)
  warning: "#FFC107",        // Желтый (Cooldown)
  destructive: "#F44336",    // Красный (ошибки)
  muted: "#9E9E9E",          // Серый (Completed раунд)
  background: "#FFFFFF",     // Основной фон
  foreground: "#212121",     // Основной текст
}
```

**Применение:**
- **Primary (#2196F3)** - основные кнопки, активные ссылки, ID раундов
- **Success (#4CAF50)** - статус "Активен", успешные действия
- **Warning (#FFC107)** - статус "Cooldown", предупреждения
- **Destructive (#F44336)** - ошибки, статус невалидный
- **Muted (#9E9E9E)** - статус "Завершен", вторичный текст

### 2.2 Типографика

```css
/* Заголовки */
h1 { font-size: 2rem; line-height: 2.5rem; font-weight: 700; }      /* 32px */
h2 { font-size: 1.5rem; line-height: 2rem; font-weight: 600; }      /* 24px */
h3 { font-size: 1.25rem; line-height: 1.75rem; font-weight: 600; }  /* 20px */

/* Текст */
.text-base { font-size: 1rem; line-height: 1.5rem; }     /* 16px - основной */
.text-sm { font-size: 0.875rem; line-height: 1.25rem; }  /* 14px - метаданные */
.text-lg { font-size: 1.125rem; line-height: 1.75rem; }  /* 18px - акценты */

/* Шрифт */
font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
```

### 2.3 Spacing System

```javascript
// Tailwind spacing (rem units)
spacing: {
  1: '0.25rem',   // 4px
  2: '0.5rem',    // 8px
  3: '0.75rem',   // 12px
  4: '1rem',      // 16px
  6: '1.5rem',    // 24px
  8: '2rem',      // 32px
  12: '3rem',     // 48px
  16: '4rem',     // 64px
}
```

**Использование:**
- **Gap между элементами:** `gap-4` (16px)
- **Padding карточек:** `p-6` (24px)
- **Margin между секциями:** `mb-8` (32px)
- **Padding кнопок:** `px-4 py-2` (16px x 8px)

### 2.4 Responsive Breakpoints

```javascript
screens: {
  'sm': '640px',   // Мобильный (портрет)
  'md': '768px',   // Планшет
  'lg': '1024px',  // Десктоп
  'xl': '1280px',  // Широкий экран
}
```

**Стратегия:** Mobile-first
**Подход:** `min-w-full md:max-w-2xl lg:max-w-4xl`

### 2.5 Компоненты shadcn/ui

| Компонент | Применение | Файл |
|-----------|-----------|------|
| **Button** | Все кнопки (Login, Create Round, Tap) | `components/ui/button.tsx` |
| **Input** | Поля Username, Password | `components/ui/input.tsx` |
| **Card** | Карточки раундов, форма логина | `components/ui/card.tsx` |
| **Toast** | Уведомления об ошибках/успехе | `sonner` |
| **Dialog** | Модальное окно создания раунда (опционально) | `components/ui/dialog.tsx` |

**Пример использования:**
```tsx
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";
import { toast } from "sonner";

<Button variant="default" size="lg">Войти</Button>
<Input type="text" placeholder="Имя пользователя" />
<Card><CardContent>...</CardContent></Card>
toast.error("Неверный пароль");
```

---

## 3. Wireframes (ASCII)

### 3.1 Страница Login (`/login`)

```
┌─────────────────────────────────────────────────┐
│                    ВОЙТИ                        │
├─────────────────────────────────────────────────┤
│                                                 │
│  Имя пользователя:                              │
│  ┌───────────────────────────────────────────┐  │
│  │ Введите имя                               │  │ <- Input (focus:ring-2)
│  └───────────────────────────────────────────┘  │
│                                                 │
│  Пароль:                                        │
│  ┌───────────────────────────────────────────┐  │
│  │ ••••••••                                  │  │ <- Input type="password"
│  └───────────────────────────────────────────┘  │
│                                                 │
│  [❌ Неверный пароль]                           │ <- Текст красный (появляется при ошибке)
│                                                 │
│  ┌───────────────────────────────────────────┐  │
│  │             Войти                         │  │ <- Button primary, w-full
│  └───────────────────────────────────────────┘  │
│                                                 │
└─────────────────────────────────────────────────┘

Центрирование: max-w-md mx-auto mt-16
```

**Состояния:**
- **Idle:** Кнопка активна
- **Loading:** Кнопка disabled, показывает спиннер
- **Error:** Красный текст под кнопкой

---

### 3.2 Страница Список раундов (`/rounds`)

```
┌─────────────────────────────────────────────────────────────────┐
│  Список РАУНДОВ                              Имя: admin    [⎕]  │ <- Header sticky top-0
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐                                            │
│  │ + Создать раунд │  <- Button (только role=admin)             │
│  └─────────────────┘                                            │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  🟢 Round ID: 8c3eed83-8a8a-41a0-8f91-9ad501e8f8a1       │  │ <- Card, ID = Link (text-primary)
│  │                                                           │  │
│  │  📅 Start: 17.11.2025, 15:00:00                           │  │ <- text-sm text-muted-foreground
│  │  🏁 End:   17.11.2025, 15:01:00                           │  │
│  │                                                           │  │
│  │  ───────────────────────────────────────────────────────  │  │
│  │  Статус: Активен                                          │  │ <- Badge (bg-success)
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  🟡 Round ID: 8c3eed83-8a8a-41a0-8f91-9ad501e8f8a2       │  │
│  │                                                           │  │
│  │  📅 Start: 17.11.2025, 16:00:00                           │  │
│  │  🏁 End:   17.11.2025, 16:01:00                           │  │
│  │                                                           │  │
│  │  ───────────────────────────────────────────────────────  │  │
│  │  Статус: Cooldown (00:45)                                 │  │ <- Badge (bg-warning)
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  ⚫ Round ID: 8c3eed83-8a8a-41a0-8f91-9ad501e8f8a3       │  │
│  │                                                           │  │
│  │  📅 Start: 17.11.2025, 14:00:00                           │  │
│  │  🏁 End:   17.11.2025, 14:01:00                           │  │
│  │                                                           │  │
│  │  ───────────────────────────────────────────────────────  │  │
│  │  Статус: Завершен | Победитель: Иван (105 очков)         │  │ <- text-muted-foreground
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

Layout: max-w-4xl mx-auto p-6
Grid: flex flex-col gap-4
```

**Статусы раундов:**
- 🟢 **Active** → `bg-success text-white`
- 🟡 **Cooldown** → `bg-warning text-black`
- ⚫ **Completed** → `bg-muted text-muted-foreground`

---

### 3.3 Страница Раунда - Состояние COOLDOWN (`/rounds/:id`)

```
┌───────────────────────────────────────────────────────┐
│  ← Раунды                              Имя: Иван  [⎕] │ <- Header с Back button
├───────────────────────────────────────────────────────┤
│                                                       │
│              ░░░░░░░░░░░░░░░                          │
│            ░░▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░                        │
│          ░░▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░                      │ <- ASCII Goose
│          ░░▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░                      │    (grayscale, не кликабелен)
│        ░░░░▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░                    │
│      ░░▒▒▒▒░░░░▓▓▓▓▓▓▓▓▓▓▓▓░░░░▒▒▒▒░░                │
│      ░░▒▒▒▒▒▒▒▒░░░░░░░░░░░░▒▒▒▒▒▒▒▒░░                │
│      ░░▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒░░                │
│        ░░▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒░░                  │
│          ░░░░░░░░░░░░░░░░░░░░░░░░░░                  │
│                                                       │
│  ┌─────────────────────────────────────────────────┐  │
│  │           ⏳ COOLDOWN                           │  │ <- Card (bg-warning/10)
│  │                                                 │  │
│  │     До начала раунда: 00:15                     │  │ <- text-4xl font-bold
│  │                                                 │  │    (обновляется каждую секунду)
│  └─────────────────────────────────────────────────┘  │
│                                                       │
│  Round ID: 8c3eed83-...                               │ <- text-sm text-muted
│  Start: 17.11.2025, 15:00:00                          │
│  End:   17.11.2025, 15:01:00                          │
│                                                       │
└───────────────────────────────────────────────────────┘

Goose размер: w-64 h-64 mx-auto
Таймер: useCountdown hook, обновление 1s
```

---

### 3.4 Страница Раунда - Состояние ACTIVE (`/rounds/:id`)

```
┌───────────────────────────────────────────────────────┐
│  ← Раунды                              Имя: Иван  [⎕] │
├───────────────────────────────────────────────────────┤
│                                                       │
│              ░░░░░░░░░░░░░░░                          │
│            ░░▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░                        │
│          ░░▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░                      │ <- КЛИКАБЕЛЬНО!
│          ░░▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░                      │    cursor-pointer
│        ░░░░▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░                    │    hover:scale-105
│      ░░▒▒▒▒░░░░▓▓▓▓▓▓▓▓▓▓▓▓░░░░▒▒▒▒░░                │    active:scale-95
│      ░░▒▒▒▒▒▒▒▒░░░░░░░░░░░░▒▒▒▒▒▒▒▒░░                │    transition-all
│      ░░▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒░░                │
│        ░░▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒░░                  │
│          ░░░░░░░░░░░░░░░░░░░░░░░░░░                  │
│                                                       │
│  ┌─────────────────────────────────────────────────┐  │
│  │           🟢 РАУНД АКТИВЕН!                     │  │ <- Card (bg-success/10)
│  │                                                 │  │
│  │     До конца раунда: 00:23                      │  │ <- text-4xl font-bold
│  │                                                 │  │
│  │     Мои очки: 123                               │  │ <- text-2xl (обновляется при тапе)
│  └─────────────────────────────────────────────────┘  │
│                                                       │
└───────────────────────────────────────────────────────┘

Интерактив:
- onClick гуся → POST /rounds/:id/tap
- Оптимистичное обновление: points++ сразу
- Rate limit: 10 тапов/сек (лимит на клиенте тоже)
- Тактильный фидбек: scale анимация
```

---

### 3.5 Страница Раунда - Состояние COMPLETED (`/rounds/:id`)

```
┌───────────────────────────────────────────────────────┐
│  ← Раунды                              Имя: Иван  [⎕] │
├───────────────────────────────────────────────────────┤
│                                                       │
│              ░░░░░░░░░░░░░░░                          │
│            ░░▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░                        │
│          ░░▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░                      │ <- Grayscale
│          ░░▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░                      │    filter: grayscale(100%)
│        ░░░░▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░                    │    opacity-60
│      ░░▒▒▒▒░░░░▓▓▓▓▓▓▓▓▓▓▓▓░░░░▒▒▒▒░░                │
│      ░░▒▒▒▒▒▒▒▒░░░░░░░░░░░░▒▒▒▒▒▒▒▒░░                │
│      ░░▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒░░                │
│        ░░▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒▒░░                  │
│          ░░░░░░░░░░░░░░░░░░░░░░░░░░                  │
│                                                       │
│  ┌─────────────────────────────────────────────────┐  │
│  │           ⚫ РАУНД ЗАВЕРШЕН                      │  │ <- Card (bg-muted/10)
│  ├─────────────────────────────────────────────────┤  │
│  │  Всего тапов:    999999                         │  │ <- text-muted-foreground
│  │  Всего очков:    123456                         │  │
│  │                                                 │  │
│  │  🏆 Победитель:  Иван                           │  │ <- text-lg font-semibold
│  │     Очков:       100500                         │  │
│  │                                                 │  │
│  │  ───────────────────────────────────────────────│  │
│  │  Мои результаты:                                │  │
│  │     Тапов:       321                            │  │ <- text-base
│  │     Очков:       345                            │  │
│  └─────────────────────────────────────────────────┘  │
│                                                       │
└───────────────────────────────────────────────────────┘

Примечания:
- Если игрок НЕ участвовал → "Вы не участвовали в этом раунде"
- Если роль=nikita → "Мои очки: 0" (всегда)
```

---

## 4. User Flow Diagrams

### 4.1 Flow: Новый пользователь

```
START
  ↓
[Открывает /login]
  ↓
[Вводит username="Иван", password="pass1234"]
  ↓
[Нажимает "Войти"]
  ↓
POST /auth/login ──────┐
  ↓                    │ (создает нового user)
[Получает JWT]         │ role=survivor
  ↓                    │
localStorage.setItem('token', jwt)
  ↓
[Redirect → /rounds]
  ↓
[Видит список раундов]
  ↓
[Кликает на Round ID]
  ↓
[Redirect → /rounds/:id]
  ↓
END
```

### 4.2 Flow: Администратор создает раунд

```
START (role=admin)
  ↓
[/rounds]
  ↓
[Видит кнопку "Создать раунд"]
  ↓
[Нажимает кнопку]
  ↓
[Открывается Dialog/форма]
  ↓
[Указывает startTime (через 2 минуты)]
  ↓
POST /rounds { startTime }
  ↓
[Создается раунд, status=cooldown]
  ↓
[Redirect → /rounds/:newRoundId]
  ↓
[Видит обратный отсчет "00:120"]
  ↓
[Ждет startTime]
  ↓
[Статус меняется → active]
  ↓
[Гусь становится кликабельным]
  ↓
END
```

### 4.3 Flow: Игрок участвует в раунде

```
START
  ↓
[/rounds/:id] (status=active)
  ↓
[Видит гуся + "Мои очки: 0"]
  ↓
┌──────────────────┐
│  ЦИКЛ ТАПОВ      │
│  ↓               │
│ [Кликает гуся]   │
│  ↓               │
│ POST /rounds/:id/tap
│  ↓               │
│ [points++ локально] (оптимистично)
│  ↓               │
│ [Сервер отвечает: points=28]
│  ↓               │
│ [Обновляет UI: "Мои очки: 28"]
│  ↓               │
│ [Повторяет]      │
└──────────────────┘
  ↓
[Таймер → 00:00]
  ↓
[Статус → completed]
  ↓
[Гусь grayscale, не кликабелен]
  ↓
[Показывается лидерборд]
  ↓
END
```

---

## 5. Layout Specifications

### 5.1 Login Page

**Контейнер:**
```tsx
<div className="min-h-screen bg-background flex items-center justify-center p-4">
  <Card className="w-full max-w-md">
    {/* Форма */}
  </Card>
</div>
```

**Компоненты:**
- `CardHeader` → `<h1>ВОЙТИ</h1>`
- `CardContent` → форма с полями
- `Input` → `className="mb-4"`
- `Button` → `className="w-full mt-6"`

### 5.2 Rounds List Page

**Header:**
```tsx
<header className="sticky top-0 z-10 bg-background border-b p-4">
  <div className="max-w-4xl mx-auto flex justify-between items-center">
    <h1 className="text-2xl font-bold">Список РАУНДОВ</h1>
    <span className="text-sm text-muted-foreground">Имя: {username}</span>
  </div>
</header>
```

**Content:**
```tsx
<main className="max-w-4xl mx-auto p-6">
  {isAdmin && <CreateRoundButton />}
  <div className="flex flex-col gap-4 mt-6">
    {rounds.map(round => <RoundCard key={round.id} round={round} />)}
  </div>
</main>
```

**RoundCard компонент:**
```tsx
<Card className="hover:shadow-lg transition-shadow">
  <CardContent className="p-6">
    <Link to={`/rounds/${round.id}`} className="text-primary hover:underline">
      ● Round ID: {round.id}
    </Link>
    <p className="text-sm text-muted-foreground mt-2">
      📅 Start: {formatDate(round.startTime)}
    </p>
    <p className="text-sm text-muted-foreground">
      🏁 End: {formatDate(round.endTime)}
    </p>
    <div className="mt-4 pt-4 border-t">
      <StatusBadge status={round.status} />
    </div>
  </CardContent>
</Card>
```

### 5.3 Round Page

**Layout:**
```tsx
<div className="min-h-screen bg-background">
  <header className="border-b p-4">
    <div className="max-w-4xl mx-auto flex items-center gap-4">
      <Button variant="ghost" onClick={() => navigate('/rounds')}>
        ← Раунды
      </Button>
      <span className="ml-auto text-sm">Имя: {username}</span>
    </div>
  </header>

  <main className="max-w-2xl mx-auto p-6 text-center">
    <GooseImage
      clickable={status === 'active'}
      onTap={handleTap}
      className="w-64 h-64 mx-auto mb-8"
    />

    <RoundStatusCard
      status={status}
      timeRemaining={timeRemaining}
      myPoints={myStats?.points}
      winner={winner}
      totalStats={totalStats}
    />
  </main>
</div>
```

---

## 6. Component Specifications

### 6.1 StatusBadge

```tsx
interface StatusBadgeProps {
  status: 'active' | 'cooldown' | 'completed';
  countdown?: number; // секунды
}

// Варианты:
active → bg-success text-white "🟢 Активен"
cooldown → bg-warning text-black "🟡 Cooldown (MM:SS)"
completed → bg-muted text-muted-foreground "⚫ Завершен"
```

### 6.2 GooseImage

```tsx
interface GooseImageProps {
  clickable: boolean;
  onTap?: () => void;
  className?: string;
}

// Стили:
clickable=true → cursor-pointer hover:scale-105 active:scale-95
clickable=false → filter grayscale(100%) opacity-60 cursor-not-allowed
```

### 6.3 Countdown

```tsx
interface CountdownProps {
  targetTime: Date;
  onComplete?: () => void;
}

// Формат: MM:SS
// Обновление: каждую секунду (setInterval)
// Цвет: text-4xl font-bold
```

### 6.4 CreateRoundButton

```tsx
// Показывается только если role=admin
// onClick → открывает Dialog с DateTimePicker
// POST /rounds { startTime }
// После успеха → navigate(`/rounds/${newRound.id}`)
```

---

## 7. Responsive Design

### Breakpoints

```css
/* Mobile First */
.container {
  width: 100%;           /* < 640px */
  max-width: 640px;      /* sm: >= 640px */
  max-width: 768px;      /* md: >= 768px */
  max-width: 1024px;     /* lg: >= 1024px */
}
```

### Адаптации

**Login Page:**
- Mobile: `p-4`, full-width
- Desktop: `max-w-md`, центрировано

**Rounds List:**
- Mobile: single column, `gap-4`
- Desktop: same (вертикальный список)

**Round Page:**
- Mobile: Гусь `w-48 h-48`
- Desktop: Гусь `w-64 h-64`

---

## 8. Interactions & Animations

### 8.1 Button States

```css
/* Default */
.btn-primary {
  @apply bg-primary text-primary-foreground;
  @apply hover:bg-primary/90;
  @apply active:scale-95;
  @apply disabled:opacity-50 disabled:cursor-not-allowed;
  @apply transition-all duration-150;
}
```

### 8.2 Tap Animation

```tsx
// При клике на гуся
<div
  onClick={handleTap}
  className="
    transition-transform duration-100
    hover:scale-105
    active:scale-95
  "
>
  {/* Goose ASCII art */}
</div>

// + Тактильный фидбек:
// 1. Визуально: scale анимация
// 2. Звук: (опционально) tap.mp3
// 3. Haptic: (мобильный) navigator.vibrate(10)
```

### 8.3 Status Transitions

```tsx
// Автоматическое изменение UI при смене статуса
useEffect(() => {
  const interval = setInterval(() => {
    const now = Date.now();
    if (now >= round.startTime && now < round.endTime) {
      setStatus('active');
    } else if (now >= round.endTime) {
      setStatus('completed');
    }
  }, 1000);

  return () => clearInterval(interval);
}, [round]);
```

### 8.4 Toast Notifications

```tsx
// Успех
toast.success("Вход выполнен успешно", {
  duration: 2000,
  position: 'top-center',
});

// Ошибка
toast.error("Неверный пароль", {
  duration: 3000,
  position: 'top-center',
});

// Rate limit
toast.warning("Слишком быстро! Подождите секунду", {
  duration: 1500,
});
```

---

## 9. Accessibility (A11y)

### Keyboard Navigation
- **Tab** - переход между элементами
- **Enter** - активация кнопок/ссылок
- **Space** - клик по гусю (когда в фокусе)

### ARIA attributes
```tsx
<Button aria-label="Войти в систему">Войти</Button>
<Input aria-describedby="username-hint" />
<div role="status" aria-live="polite">{countdown}</div>
```

### Screen Reader Support
- Все интерактивные элементы имеют labels
- Статус раунда озвучивается при изменении
- Ошибки формы озвучиваются сразу

---

## 10. Performance

### Оптимизации

1. **Code Splitting:**
   ```tsx
   const RoundPage = lazy(() => import('./pages/RoundPage'));
   ```

2. **Memo для Goose:**
   ```tsx
   export const GooseImage = memo(({ clickable, onTap }) => {
     // Не перерисовывается если props не изменились
   });
   ```

3. **Debounce для тапов:**
   ```tsx
   const handleTap = useMemo(
     () => throttle(() => tapService.tap(roundId), 100),
     [roundId]
   );
   ```

4. **Optimistic Updates:**
   ```tsx
   const [localPoints, setLocalPoints] = useState(0);

   const handleTap = async () => {
     setLocalPoints(prev => prev + 1); // Мгновенно
     const { points } = await tapService.tap(roundId);
     setLocalPoints(points); // Синхронизация
   };
   ```

---

## 11. Error States

### Сетевые ошибки
```tsx
// Компонент ErrorBoundary
<ErrorBoundary fallback={<ErrorPage />}>
  <App />
</ErrorBoundary>

// Toast при network error
toast.error("Ошибка подключения. Проверьте интернет");
```

### Форма логина
```tsx
{error && (
  <p className="text-destructive text-sm mt-2">
    {error}
  </p>
)}
```

### Пустое состояние (No rounds)
```tsx
{rounds.length === 0 && (
  <Card className="text-center p-12">
    <p className="text-muted-foreground">
      Раунды еще не созданы
    </p>
  </Card>
)}
```

---

## 12. Примеры кода

### Login Page (полный компонент)

```tsx
import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/card';
import { toast } from 'sonner';
import { authService } from '@/services/auth.service';

export const LoginPage = () => {
  const [username, setUsername] = useState('');
  const [password, setPassword] = useState('');
  const [error, setError] = useState('');
  const [loading, setLoading] = useState(false);
  const navigate = useNavigate();

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setError('');

    if (password.length < 8) {
      setError('Пароль должен содержать минимум 8 символов');
      return;
    }

    setLoading(true);
    try {
      const { accessToken } = await authService.login({ username, password });
      localStorage.setItem('token', accessToken);
      toast.success('Вход выполнен успешно');
      navigate('/rounds');
    } catch (err: any) {
      if (err.response?.status === 401) {
        setError('Неверный пароль');
      } else {
        toast.error('Ошибка подключения');
      }
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="min-h-screen bg-background flex items-center justify-center p-4">
      <Card className="w-full max-w-md">
        <CardHeader>
          <CardTitle className="text-center text-2xl">ВОЙТИ</CardTitle>
        </CardHeader>
        <CardContent>
          <form onSubmit={handleSubmit} className="space-y-4">
            <div>
              <label className="text-sm font-medium">Имя пользователя:</label>
              <Input
                type="text"
                value={username}
                onChange={(e) => setUsername(e.target.value)}
                placeholder="Введите имя"
                required
                autoFocus
              />
            </div>

            <div>
              <label className="text-sm font-medium">Пароль:</label>
              <Input
                type="password"
                value={password}
                onChange={(e) => setPassword(e.target.value)}
                placeholder="Введите пароль"
                required
              />
            </div>

            {error && (
              <p className="text-destructive text-sm">{error}</p>
            )}

            <Button
              type="submit"
              className="w-full"
              disabled={loading}
            >
              {loading ? 'Загрузка...' : 'Войти'}
            </Button>
          </form>
        </CardContent>
      </Card>
    </div>
  );
};
```

---

## Итого

Документ предоставляет:
- ✅ Полный Design System (цвета, типографика, отступы)
- ✅ ASCII wireframes для всех 3 страниц и состояний
- ✅ User flow диаграммы
- ✅ Спецификации компонентов
- ✅ Responsive стратегию
- ✅ Примеры кода

**Следующие шаги:**
1. Настроить shadcn/ui + Tailwind
2. Реализовать компоненты UI
3. Подключить к API
4. Тестирование на разных экранах

**Ссылки:**
- Полный PRD: `/docs/prd.md`
- shadcn/ui: https://ui.shadcn.com
- Tailwind CSS: https://tailwindcss.com
